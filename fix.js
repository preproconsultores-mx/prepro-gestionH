const fs = require('fs'); let content = fs.readFileSync('app.js', 'utf8'); const wrapper = unction safeStorageSet(key, value) {\n  try {\n    localStorage.setItem(key, value);\n  } catch (e) {\n    console.error('Storage error:', e);\n    alert('?? ERROR DE MEMORIA: No se pudo guardar. Si adjuntaste un PDF, es muy pesado o has alcanzado el límite del navegador. Borra pólizas en histórico o no adjuntes archivos tan pesados.');\n  }\n}\n; content = content.replace(/localStorage\.setItem\(/g, 'safeStorageSet('); content = content.replace('const FIREBASE_REST_URL =', wrapper + '\nconst FIREBASE_REST_URL ='); fs.writeFileSync('app.js', content);
